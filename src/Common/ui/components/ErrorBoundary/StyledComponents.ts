import styled from 'styled-components'

export const Fallback = styled.div`
  padding: 1.5rem;
  border: 1px solid #3f2330;
  border-radius: 0.75rem;
  background: rgba(248, 113, 113, 0.08);
  color: var(--color-danger-text);
  text-align: center;
`